type Query {
    shows(titleFilter: String): [Show]
    showsByArtist(artistFilter: String): [Show]
}

type Show {
    title: String
    releaseYear: Int
    artist: Artist
}

type Artist {
    name: String
    age: Int
}

